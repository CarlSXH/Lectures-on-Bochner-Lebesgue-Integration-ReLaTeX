

\usepackage{enumitem}
\usepackage{titlesec}

\usepackage{fancyhdr}
\usepackage{indentfirst}


% allows " to be the correct quotation marks
% already fixed all quotation marks
%\usepackage [autostyle, english = american]{csquotes}
%\MakeOuterQuote{"}

% allows clincking, hyperref
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,      
    urlcolor=cyan,
    pdftitle={Lectures on Bochner-Lebesgue Integration},
    pdfpagemode=FullScreen,
    }



% header and footer for each page
\pagestyle{fancy}
\setlength{\headheight}{15pt}
\renewcommand{\headrulewidth}{2pt}
\renewcommand{\footrulewidth}{1pt}
\fancyhead[RE,LO]{Page \thepage}
\fancyhead[LE,RO]{\scshape\nouppercase\rightmark}
\fancyfoot[CE,CO]{\scshape\nouppercase\leftmark}

% header/footer style for start of chapters
\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{Page \thepage}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0.4pt}
}


% verticle stretch between lines
\usepackage{setspace}
\linespread{1.1}


%allows stretching and line breaks
\binoppenalty=100
\relpenalty=100
\sloppy

% reducing the size of section title
\RequirePackage{fix-cm}
\makeatletter 
\newcommand\semiHuge{\@setfontsize\semiHuge{15}{18}}
\makeatother
\titleformat*{\section}{\fontsize{17}{20.4}\selectfont\bfseries}


% sections numbered by capital letters
\renewcommand{\thesection}{\Alph{section}}

% enum numbered by letters
\renewcommand{\theenumi}{\alph{enumi})}
\setlist[enumerate,1]{label={\alph*)},ref=\thedefinition\alph*)}
\setlist[enumerate,2]{label={\alph*)},ref=\thedefinition\alph*)}

% numbering of theorems is chapter then number, reset only between chanters
\renewcommand{\thedefinition}{\arabic{chapter}.\arabic{definition}}